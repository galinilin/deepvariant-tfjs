# If you decide to commit the model artifacts directly (instead of relying on
# `npm run sync-models` to populate them locally), uncomment the lines below
# and run `git lfs install && git lfs track` before committing.
#
# public/models/**/*.bin   filter=lfs diff=lfs merge=lfs -text
# public/fixtures/*.npy    filter=lfs diff=lfs merge=lfs -text
